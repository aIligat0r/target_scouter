favicons:
  sha256hash: ["<STR: sha256 hash favicon>"]
  murmurhash: [<INT: mmh3 hash favicon>]
  enable: False

keywords:
  title: ["<title keyword>"]
  enable: False

ips:
  ips: ["<ip>"]
  enable: False